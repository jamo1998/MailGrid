import React from 'react';

const Landing = () => {
  return (
    //2 curley braces, 1st one indicates were about to pass in
    //some javaScript, 2nd represents the object we're passing
    <div style={{ textAlign: 'center' }}>
      <h1>
        MailGrid!
      </h1>
      Collect feedback from your users
    </div>
  );
};

export default Landing;